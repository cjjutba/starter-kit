# assets

Files the code reads at build or request time rather than serving as they
are. `Geist-Medium.ttf` is a copy of the file the `geist` package ships,
under the SIL Open Font License, because the Open Graph image is drawn on
the server from font data and Vercel traces a file read from here.
