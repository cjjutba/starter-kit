# brand

The mark, the lockup, the favicon and the app icon, all as SVG. The geometry
is the source of truth. Never redraw it, never let a model redraw it. When a
size is needed, export from the SVG.

Until they exist, `public/icons/` holds two solid placeholders so the
manifest is valid. The `image` skill replaces them.
