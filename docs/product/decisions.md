# Decision log

Append only. When a decision changes, add a new entry that supersedes the old one
rather than editing history. Each entry says what was decided, what the
alternatives were, and why this one. Dates are headings.

---
